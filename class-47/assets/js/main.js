(function($){
	"use strict";
	jQuery(document).ready(function($)
	{
	$(".testimonial-carousel-active").owlCarousel({
		items:1,
		loop:true,
		dots:true,
		nav:false,
		autoplay:true,
	    smartSpeed:3000,
		});
		
		});
				
				jQuery(window).load(function(){})})
				
	(jQuery);